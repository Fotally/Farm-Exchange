# Windows 构建与验收

## 构建来源

- 引擎：`E:\Godot\Godot_v4.7.2-stable_mono_win64` 中的 Godot 4.7.2 Mono。
- 官方 .NET 导出模板：同一目录中的 `Godot_v4.7.2-stable_mono_export_templates.tpz`；Windows x86_64 模板与中文所需 ICU 数据解压在 `export_templates/4.7.2.stable.mono/`，可供后续项目复用。
- `project.godot` 启用中文断行所需的文本服务数据；导出包应包含该数据。
- 项目导出配置：`export_presets.cfg`；C# 构建配置：`FarmExchange.sln`、`FarmExchange.csproj`、`NuGet.Config`。Godot 的 .NET 导出要求项目根目录有与程序集同名的 `.sln`，Windows 发布首次运行还需从 nuget.org 获取 .NET 运行时包。

## 生成可运行版本

在项目根目录执行：

```text
dotnet build FarmExchange.csproj -c Release
E:\Godot\Godot_v4.7.2-stable_mono_win64\Godot_v4.7.2-stable_mono_win64_console.exe --headless --path . --export-release Windows build/windows/FarmExchange.exe
```

导出后运行 `build/windows/FarmExchange.exe`。`build/FarmExchange-Windows.zip` 是同一目录的打包版；发布或移动时保持解压后的整个目录完整。

## 首版验收步骤

1. 运行游戏，确认出现等距地图和右侧经营按钮；左键短按选格，左键拖动、滚轮和 WASD 可浏览地图。移动到地图边缘时，确认可看到菱形边界，边界外没有地块，镜头中心不能移出地图范围。
2. 任意选两格土地，免费解锁；分别建造农田与磨坊。两次免费机会用完后，新土地应需要 10 金币。
3. 建好农田后无需手动播种和浇水：第 1 tick 工人播种，第 2 tick 工人浇水，之后再过 5 tick 小麦自动收获并交给空闲磨坊。此时商店不能出售小麦。
4. 再等 10 秒，面粉库存增加 1；在商店出售，金币增加 5。重复一次后可花 10 金币解锁任意另一格土地。
5. 移除正在加工的磨坊，已投入的小麦应消失；移除生长中的农田，未收获小麦应消失。土地保持已解锁。

当前版本为首个可玩循环；存档与离线收益的规则尚待确认，退出游戏后本次进度不会保留。
