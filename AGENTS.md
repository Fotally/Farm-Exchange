# 项目背景

- 本项目是 Godot 放置挂机游戏。地图为 128×128 格，采用等距视角。
- 已确认的核心循环：玩家建造农田，1 名工人按 tick 自动播种和浇水；小麦成熟后由磨坊加工成面粉，面粉在商店出售，收入用于解锁更多土地。
- 首版仅有小麦作物；每座磨坊自动用 10 tick 将 1 份小麦加工成 1 份面粉，加工、建造和移除磨坊均免费。只有面粉能出售，每份售价 5 金币。
- Godot 引擎目录：`E:\Godot\Godot_v4.7.2-stable_mono_win64`。
- 用户使用中文；项目文档、设计讨论与代码修改说明使用中文。

# 代码系统结构

- `scripts/FarmGame.cs`：游戏状态模块。持有固定 128×128 土地、建筑、作物、工人轮流工作、tick 推进、库存、交易和购地规则；界面通过格子快照及经营命令使用它。
- `scripts/WorldMap.cs`：地图表现模块。绘制等距网格和边缘，换算与选择格坐标，提供菱形地图内的镜头限制；不维护经营规则。
- `scripts/CameraController.cs`：输入与镜头模块。区分左键点击、左键拖动，处理缩放和键盘移动；通过 `WorldMap` 的接口选择格子及限制镜头。
- `scripts/Main.cs` 与 `scenes/main.tscn`：场景协调及界面。把按钮命令和一秒计时器交给 `FarmGame`，再刷新地图与状态栏。
- `tests/`：通过对应场景检查地图、经营规则和主场景交互；`export_presets.cfg` 定义 Windows 验收构建。

# 工作约定

1. 每次代码修改完成后，在同一任务中及时更新 `docs/` 对应的中文文档与本文件的系统结构；代码、规则、操作说明和验收步骤一致后，才视为修改完成。
2. 实施前核对 `docs/游戏系统规划.md` 中的已确认范围。具体数值和操作规则未确认前仅做不依赖它们的工作；需要变更已沟通的实施方案时，先与用户沟通。
3. 保持模块接口简洁；只为实际出现的需求建立模块与接缝，不为假想情况增加兜底或抽象层。
4. 使用项目指定的 Godot 引擎验证场景与脚本。引擎可执行文件位于上述目录。
5. GitHub issue 是所有修改的唯一入口：Agent 实施前必须找到或创建写明范围和验收标准的 issue，只完成该 issue 记录的内容；实现与验证均在 `dev` 分支进行，完成后创建关联 issue 的 `dev` → `main` Pull Request，并等待人工合并。Agent 不直接提交或推送 `main`，也不自行合并 Pull Request。

# 文档索引

- `docs/游戏系统规划.md`：已确认背景、待确认决策、系统模块与分阶段交付范围。
- `docs/地图与交互.md`：地图渲染、坐标换算、镜头及交互的实际实现说明；相关代码出现后维护。
- `docs/生产与交易.md`：已确认的首版数值、生产出售购地规则与代码对应。
- `docs/构建与验收.md`：Windows 导出配置、引擎模板位置与可运行版本的验收步骤。
- `docs/GitHub协作流程.md`：issue、`dev` 开发分支、`main` 保护分支及人工合并流程。
